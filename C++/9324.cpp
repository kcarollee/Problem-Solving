#include <iostream>
#include<bits/stdc++.h>
#include <cstring>

class Crypt
{
private:
	char *str;
	
public:
	Crypt (char *array)
	{
		int len;
		
		len = strlen(array);
		str = new char[len + 1];
		strcpy(str, array);

	}

	bool Decipher();
	void sortString(string
};

inline bool Crypt::Decipher()
{

}